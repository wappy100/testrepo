name: skip test
on:
  pull_request:

jobs:
  first:
    name: first
    runs-on: ubuntu-latest
    steps:
      - run: exit 1
  second:
    name: second
    needs: first
    runs-on: ubuntu-latest
    steps:
      - run: echo ok
